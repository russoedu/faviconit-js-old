extends templates/layout
//- http://rajasegar.github.io/PUG-Bootstrap/components.html#buttons
block content
  div.card#description
    //- img.card-img-top(src='/img/main-image.png', alt='Created with Faviconit')
    div.card-body
      h5.card-title!= __('homeHighlight')
      p.card-text!= __('homeSubHighlight')

  div.card
    div.card-header.text-center= __('fillTheForm')
    div.card-body
      form
        div.alert.alert-danger.alert-dismissable
          button.close(type='button' data-dismiss='alert') &times;
          p ERROR
          //- @foreach($errors->all() as $error)
          //- {{ $error }}</br>
          //- @endforeach
        form(action="upload" method="post" enctype="multipart/form-data")
          input#filer_input(type="file" name="files[]" multiple="multiple")
          input(type="submit" value="Submit")
        div.form-group
          label.control-label.col-sm-3(for="iconFile")= __('faviconFile')
          div.col-sm-9
            div#upload-mask
              div#fileUploader= __('formFileChoose')
              span.help-block!= __('formFileHelp')
            div#imageDisplay
      h5.card-title Special title treatment
      p.card-text With supporting text below as a natural lead-in to additional content.
      a(href='#' class='btn btn-primary') Go somewhere
      div
        img#image(src='/img/faviconit-social-logo-3.png')
        div.docs-preview.clearfix
          div.img-preview.preview-lg(style='width: 310px; height: 310px;')
        </div)
    div.card-footer.text-muted 2 days ago
  p #{__('pageTitle')}
  p #{__('socialImage')}
  p #{__('homeHighlight')}
  p #{__('homeSubHighlight')}
  p #{__('fillTheForm')}
  p #{__('faviconName')}
  p #{__('faviconNamePlaceholder')}
  p #{__('faviconNameHelp')}
  p #{__('faviconVersionHelp')}
  p #{__('faviconLocationHelp')}
  p #{__('faviconVersion')}
  p #{__('faviconVersionPlaceholder')}
  p #{__('ogDescription')}
  p #{__('metaDescription')}
  p #{__('metaDownload')}
  p #{__('metaFileNotFound')}
  p #{__('ogTitle')}
  p #{__('faviconFile')}
  p #{__('formFileChoose')}
  p #{__('formFileDragNDrop')}
  p #{__('formFileMultiDragError')}
  p #{__('uploaderBlockMessage')}
  p #{__('formFileAbortUpload')}
  p #{__('formFileHelp')}
  p #{__('swapImageText')}
  p #{__('editImageText')}
  p #{__('uploadError')}
  p #{__('advancedAccordionColapsed')}
  p #{__('advancedAccordionExpanded')}
  p #{__('downloadReady')}
  p #{__('thanks')}
  p #{__('download')}
  p #{__('availability')}
  p #{__('downloadNotFound')}
  p #{__('goBackLink')}
  p #{__('goBackText')}
  p #{__('faviconLocation')}
  p #{__('faviconLocationPlaceholder')}
  p #{__('shareTheLove')}
  p #{__('pageNotFound')}
  p #{__('instructionsTitle')}
  p #{__('instructionThanks')}
  p #{__('instructions')}
  p #{__('instructionsBlock')}
  p #{__('helpUsTranslate')}
  p #{__('donationButton')}
